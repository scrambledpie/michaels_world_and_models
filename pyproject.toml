[project]
name = "michaels-world-and-models"
version = "0.1.0"
description = "An MkDocs blogging site."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mkdocs>=1.6.1,<2.0",
  "mkdocs-material>=9.5.0",
]

[tool.uv]
package = false
